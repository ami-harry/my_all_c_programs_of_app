// #include <stdio.h>
// #include <math.h>
// int main()
// {
//     int a, b, c;
//     double A, B, C;
//     const double PI = 3.1415926;
//     printf("Enter length of a\n");
//     scanf("%d", &a);
//     printf("Enter length of b\n");
//     scanf("%d", &b);
//     printf("Enter length of c\n");
//     scanf("%d", &c);
//     A = acos((-pow(a, 2) + pow(b, 2) + pow(c, 2)) / (2 * b * c));
//     B = acos((pow(a, 2) - pow(b, 2) + pow(c, 2)) / (2 * a * c));
//     C = acos((pow(a, 2) + pow(b, 2) - pow(c, 2)) / (2 * b * a));
//     A *= 180 / PI;
//     B *= 180 / PI;
//     C *= 180 / PI;
//     printf("Angle of A= %lf\n", A);
//     printf("Angle of B= %lf\n", B);
//     printf("Angle of C= %lf\n", C);
//     return 0;
// }

#include <stdio.h>
// #include <math.h>
// int main()
// {
// int a, b, c;
// double A, B, C;
// const double PI = 3.1415;
// printf("Enter a\n");
// scanf("%d", &a);
// printf("Enter b\n");
// scanf("%d", &b);
// printf("Enter c\n");
// scanf("%d", &c);
// A = acos((pow(b, 2) + pow(c, 2) - pow(a, 2)) / (2 * b * c));
// B = acos((pow(a, 2) + pow(c, 2) - pow(b, 2)) / (2 * a * c));
// C = acos((pow(a, 2) + pow(b, 2) - pow(c, 2)) / (2 * b * a));
// A *= 180 / PI;
// B *= 180 / PI;
// C *= 180 / PI;
// printf("The sides are a=%d, b=%d and c=%d is\n\n", a, b, c);
// printf("The angle of side A is %lf\n", A);
// printf("The angle of side B is %lf\n", B);
// printf("The angle of side C is %lf\n", C);
//
// return 0;
// // }
